# Docs are now deployed by deploy-frontend.yml (unified deploy workflow)
# This file is kept as a no-op to avoid breaking any existing references.
# Both frontend and docs are built and deployed together in a single job
# to prevent race conditions on the gh-pages branch.
name: Deploy Documentation (disabled - see deploy-frontend.yml)

on:
  workflow_dispatch:
    inputs:
      reason:
        description: 'This workflow is disabled. Use Deploy to GitHub Pages instead.'
        required: false

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Docs are deployed via the unified deploy-frontend.yml workflow"
